{
  "name": "gs-proxy",
  "version": "1.0.0",
  "description": "Proxy server for uploading images to Google Drive via Google Apps Script",
  "type": "module",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "dev": "node --watch proxy.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "24.x"
  }
}
